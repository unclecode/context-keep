# Real session transcripts. Private, and they can hold API keys people pasted.
data/
sessions/
judge/results.json

# The embedding model, 87MB, downloaded on demand.
models/

__pycache__/
*.pyc

# The film's build: dependencies, the render cache, and the rendered file.
video/node_modules/
video/out/
